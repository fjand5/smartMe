<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@dimen/padSizeLarg">
    <ListView
        android:id="@+id/ioDeviceLsv"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/green"/>
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/addDeviceBtn"
        android:src="@drawable/ic_add_black_24dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:fabSize="normal"
        app:layout_anchor="@+id/ioDeviceLsv"
        app:layout_anchorGravity="bottom|right" android:background="@color/red"
        android:layout_marginBottom="@dimen/padSize"
        android:layout_marginRight="@dimen/padSize"
        />
</androidx.coordinatorlayout.widget.CoordinatorLayout>