<?xml version="1.0" encoding="utf-8"?>

<androidx.coordinatorlayout.widget.CoordinatorLayout
    android:id="@+id/layoutIoManagerLyt"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background"
    android:padding="@dimen/padSizeLarg">

    <ListView

        android:id="@+id/alarmLsv"
        android:layout_width="match_parent"
        android:layout_height="match_parent"


        />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/addAlarmBtn"
        android:src="@drawable/ic_add_black_24dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:fabSize="normal"
        app:layout_anchor="@+id/alarmLsv"
        app:layout_anchorGravity="bottom|right"
        android:background="@color/red"
        android:layout_marginBottom="@dimen/padSize"
        android:layout_marginRight="@dimen/padSize"
        />
</androidx.coordinatorlayout.widget.CoordinatorLayout>